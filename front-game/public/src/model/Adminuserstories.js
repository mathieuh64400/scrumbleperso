export default class Adminusesrstories {

    url="";
    configuration="";
    
    constructor(url, configuration){
            this.url=url;
            this.configuration=configuration;
            // this.paquet=paquet;
    }
    
    
}